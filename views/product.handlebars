{{#each categories}}
<a href="/search?cat={{id}}">{{name}}</a> >
{{/each}}

<h1>{{name}}</h1>
<h2>${{price}} (${{shipping}} shipping)</h2>
<img src="{{image}}" style="max-width:30%; float: left">
<p>
{{description}}
</p>